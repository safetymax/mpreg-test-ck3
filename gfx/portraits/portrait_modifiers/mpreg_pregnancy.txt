mpreg_visual_pregnancy = {
	usage = game
	priority = 50

	# Stage is read from var:mpreg_months (1/4/7), not a separate trait --
	# stage traits were removed for the beta (no real "hidden trait" field
	# exists in CK3, see common/traits/mpreg_traits.txt), so the variable
	# that already carried this same information now drives these weights
	# directly instead.
	mpreg_stage_1 = {
		ignore_outfit_tags = yes
		dna_modifiers = {
			morph = {
				mode = add
				gene = gene_bs_pregnant
				template = pregnant
				value = 0.33
			}
		}
		weight = {
			base = 0
			modifier = {
				add = 100
				has_trait = mp_pregnant
				var:mpreg_months = 1
			}
		}
	}

	mpreg_stage_2 = {
		ignore_outfit_tags = yes
		dna_modifiers = {
			morph = {
				mode = add
				gene = gene_bs_pregnant
				template = pregnant
				value = 0.66
			}
		}
		weight = {
			base = 0
			modifier = {
				add = 100
				has_trait = mp_pregnant
				var:mpreg_months = 4
			}
		}
	}

	mpreg_stage_3 = {
		ignore_outfit_tags = yes
		dna_modifiers = {
			morph = {
				mode = add
				gene = gene_bs_pregnant
				template = pregnant
				value = 1.0
			}
		}
		weight = {
			base = 0
			modifier = {
				add = 100
				has_trait = mp_pregnant
				var:mpreg_months = 7
			}
		}
	}
}

##############################################################################
# Cloak clearance test.
#
# gene_bs_cloak_offset's "cloak_offset" template already has a male setting
# (unlike gene_bs_pregnant, which only had female) -- no gene file edit
# needed here, just an additional value on top of it, the same mode=add
# pattern as the belly itself.
#
# OPEN QUESTION we don't know the answer to yet: vanilla already applies
# this gene at a flat value = 1 with no visible trigger condition, which
# may mean bs_cloak_offset already sits at its 0.0-1.0 ceiling for every
# character as a fixed baseline. If so, adding more here will clamp at 1.0
# and do nothing visible -- not an error, just no effect. Only real-game
# testing will tell us which case we're in.
##############################################################################

mpreg_cloak_clearance = {
	usage = game
	priority = 50

	mpreg_cloak_offset_pregnant = {
		ignore_outfit_tags = yes
		dna_modifiers = {
			morph = {
				mode = add
				gene = gene_bs_cloak_offset
				template = cloak_offset
				value = 0.5
			}
		}
		weight = {
			base = 0
			modifier = {
				add = 100
				has_trait = mp_pregnant
			}
		}
	}
}

# Lesson 32: mpreg_hide_female_pregnancy (which used to live here) is
# removed. It existed solely to hide a real mother's own belly while a
# flagged male spouse showed a cosmetic mirror of her pregnancy instead
# (the old mpreg.0001 "redirect" system, also removed -- see
# events/mpreg_events.txt). With no redirect, a woman's real pregnancy
# renders exactly like unmodified vanilla, always, with no suppression of
# any kind needed.
